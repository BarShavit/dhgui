import { TopologyNameFilterPipe } from './topology-name-filter.pipe';

describe('TopologyNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopologyNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
