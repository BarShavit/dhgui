import { TopologyFilterPipe } from './topology-filter.pipe';

describe('TopologyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TopologyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
