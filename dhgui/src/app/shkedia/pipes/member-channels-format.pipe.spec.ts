import { MemberChannelsFormatPipe } from './member-channels-format.pipe';

describe('MemberChannelsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberChannelsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
